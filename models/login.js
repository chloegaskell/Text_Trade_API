class login{
  constructor(username, password){
    this.username = username
    this.password = password
  }

  getLogin(){
    //access database by username and password
    //if the data is found return True if not return False
  }
}

module.export = login
